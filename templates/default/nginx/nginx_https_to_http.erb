server {
    listen 443;
    server_name <%= @domain %>;

    location ^~ /.well-known {
        alias <%= certbot_well_known_path_for(@domain) %>;
    }

    location / {
        return 301 http://<%= @domain %>:80$request_uri;
    }
}
